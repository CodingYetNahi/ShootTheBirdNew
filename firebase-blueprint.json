{
  "entities": {
    "Score": {
      "title": "Score",
      "description": "High scores and game records submitted by players",
      "type": "object",
      "properties": {
        "playerName": {
          "type": "string",
          "description": "The nickname of the player"
        },
        "score": {
          "type": "number",
          "description": "The final score achieved in the session"
        },
        "createdAt": {
          "type": "timestamp",
          "description": "When the score record was created"
        }
      },
      "required": ["playerName", "score", "createdAt"]
    },
    "MultiplayerRoom": {
      "title": "MultiplayerRoom",
      "description": "Live 1v1 multiplayer duels and battle rooms",
      "type": "object",
      "properties": {
        "roomCode": {
          "type": "string"
        },
        "hostName": {
          "type": "string"
        },
        "hostScore": {
          "type": "number"
        },
        "guestName": {
          "type": "string"
        },
        "guestScore": {
          "type": "number"
        },
        "status": {
          "type": "string"
        }
      },
      "required": ["roomCode", "hostName", "status"]
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /scores/{scoreId} {\n      allow read, create: if true;\n      allow update, delete: if false;\n    }\n    match /multiplayer_rooms/{roomId} {\n      allow read, write: if true;\n    }\n  }\n}"
}
